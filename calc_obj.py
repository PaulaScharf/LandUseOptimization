import numpy as np

# calculate mining yield
def calc_mine_yield(mine_list):

    all_yields = []
    # iterate thorugh list of mining blocks
    for mine in mine_list:

        # if status is active, add yield to all_yields
        if(status == "exploitation") {
            yield = $area * $factor
            all.yields.append(yield)
        }


    return(np.array(all_yields))
    
# calculate biomass of mining area
def calc_mine_biomass(mine_list):

    biomass_sum = []

    for mine in mine_list:
        # mines are unlikely to only have one initial land cover -> how to handle this in a table?
